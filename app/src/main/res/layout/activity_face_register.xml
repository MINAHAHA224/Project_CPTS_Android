<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black">

    <!-- 1. Camera Preview -->
    <androidx.camera.view.PreviewView
        android:id="@+id/camera_preview"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 2. Lớp phủ làm tối (Overlay) có cái lỗ tròn -->
    <!-- Ở đây dùng mẹo: 1 ảnh PNG trong suốt ở giữa, viền đen xung quanh -->
    <!-- Hoặc đơn giản hơn: Vẽ 1 cái vòng tròn định hướng -->
    <ImageView
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:src="@drawable/ic_face_frame"
        android:layout_centerInParent="true"
        android:alpha="0.8"/>

    <!-- (Bạn cần tạo file ic_face_frame.xml là một vòng tròn nét đứt hoặc liền màu trắng) -->

    <!-- 3. Hướng dẫn text -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="100dp"
        android:gravity="center">

        <TextView
            android:id="@+id/txt_instruction"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Vui lòng nhìn thẳng"
            android:textColor="@color/white"
            android:textSize="20sp"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/txt_timer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="5s"
            android:textColor="#FFC107"
            android:textSize="24sp"
            android:textStyle="bold"/>
    </LinearLayout>

    <!-- Nút Bắt đầu / Chụp -->
    <Button
        android:id="@+id/btn_start_record"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="BẮT ĐẦU QUÉT"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="40dp"
        android:backgroundTint="@color/colorPrimary"/>

</RelativeLayout>