<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <LinearLayout
        android:id="@+id/header_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_white_rounded"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Dòng 1: ID và Ngày -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/order_id"
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="Đơn hàng #123"
                android:textStyle="bold"
                android:textColor="@color/text_title"/>

            <TextView
                android:id="@+id/order_date"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="12/12/2023"
                android:textSize="12sp"
                android:textColor="@color/text_secondary"/>
        </LinearLayout>

        <!-- Dòng 2: Trạng thái -->
        <TextView
            android:id="@+id/order_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Đang giao hàng"
            android:textColor="@color/colorPrimary"
            android:background="@drawable/bg_quantity_btn"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:textSize="12sp"/>

        <!-- Dòng 3: Tổng tiền -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:layout_width="0dp"
                android:layout_weight="1"
                android:layout_height="wrap_content"
                android:text="Tổng tiền:"
                android:textColor="@color/text_secondary"/>

            <TextView
                android:id="@+id/order_total_price"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="15.000.000 đ"
                android:textColor="@color/text_price"
                android:textStyle="bold"
                android:textSize="16sp"/>
        </LinearLayout>

        <!-- Phần chi tiết (Expandable) - Chỗ này trong ListView hơi khó làm Expandable animation mượt -->
        <!-- Giải pháp đơn giản cho đồ án: Khi bấm vào item thì mở Activity chi tiết đơn hàng riêng, hoặc dùng Nested ListView (không khuyến khích) -->
        <!-- Ở đây ta sẽ hiển thị nút "Xem chi tiết" -->

        <TextView
            android:id="@+id/expand_arrow"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center"
            android:text="Bấm để xem chi tiết ▼"
            android:textSize="12sp"
            android:textColor="@color/text_hint"/>

        <!-- ListView con chứa chi tiết sản phẩm (Ẩn/Hiện) -->
        <!-- Lưu ý: ListView lồng trong ListView là tối kỵ. Nên dùng LinearLayout add view động bằng code Java nếu muốn hiển thị list con -->
        <LinearLayout
            android:id="@+id/details_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone"
            android:layout_marginTop="8dp">
            <!-- Code Java sẽ add view con vào đây -->
        </LinearLayout>

    </LinearLayout>
</LinearLayout>