<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background"
    android:orientation="vertical">

    <!-- 1. HEADER: Thông tin User -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp">

        <ImageView
            android:id="@+id/profile_avatar"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/ic_person"
            android:background="@drawable/bg_white_rounded"
            android:padding="2dp"/>

        <TextView
            android:id="@+id/profile_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Tên người dùng"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@color/text_title"/>

        <TextView
            android:id="@+id/profile_email"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="email@example.com"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"/>
    </LinearLayout>

    <!-- 2. MENU LIST: Các chức năng (ScrollView) -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Container chứa Fragment con (UpdateInfo, ChangePassword...) -->
            <!-- Đây là chỗ code cũ của bạn dùng để load fragment con -->
            <FrameLayout
                android:id="@+id/profile_fragment_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:visibility="gone"/>
            <!-- Mặc định gone, khi bấm menu thì hiện lên hoặc chuyển activity -->

            <!-- Mục: Thông tin cá nhân -->
            <TextView
                android:id="@+id/btn_edit_profile"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_white_rounded"
                android:drawableStart="@drawable/ic_person"
                android:drawablePadding="16dp"
                android:padding="16dp"
                android:text="Cập nhật thông tin"
                android:textColor="@color/text_title"
                android:textSize="16sp"
                android:gravity="center_vertical"
                android:layout_marginBottom="12dp"/>

            <!-- Mục: Đổi mật khẩu -->
            <TextView
                android:id="@+id/btn_change_password"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_white_rounded"
                android:drawableStart="@drawable/ic_password"
                android:drawablePadding="16dp"
                android:padding="16dp"
                android:text="Đổi mật khẩu"
                android:textColor="@color/text_title"
                android:textSize="16sp"
                android:gravity="center_vertical"
                android:layout_marginBottom="12dp"/>

            <!-- Mục: Lịch sử đơn hàng -->
            <TextView
                android:id="@+id/btn_order_history"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_white_rounded"
                android:drawableStart="@drawable/ic_receipt_long"
                android:drawablePadding="16dp"
                android:padding="16dp"
                android:text="Lịch sử đơn hàng"
                android:textColor="@color/text_title"
                android:textSize="16sp"
                android:gravity="center_vertical"
                android:layout_marginBottom="12dp"/>
            <!-- THÊM ĐOẠN NÀY NẾU THIẾU -->
            <!-- Mục: Liên hệ -->
            <TextView
                android:id="@+id/btn_contact_us"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_white_rounded"
                android:drawableStart="@drawable/ic_phone"
                android:drawablePadding="16dp"
                android:padding="16dp"
                android:text="Liên hệ"
                android:textColor="@color/text_title"
                android:textSize="16sp"
                android:gravity="center_vertical"
                android:layout_marginBottom="12dp"/>

            <!-- Mục: Về chúng tôi -->
            <TextView
                android:id="@+id/btn_about_us"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_white_rounded"
                android:drawableStart="@drawable/ic_home"
                android:drawablePadding="16dp"
                android:padding="16dp"
                android:text="Về chúng tôi"
                android:textColor="@color/text_title"
                android:textSize="16sp"
                android:gravity="center_vertical"
                android:layout_marginBottom="12dp"/>

            <!-- Mục: Đăng xuất -->
            <TextView
                android:id="@+id/btn_logout"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_white_rounded"
                android:drawableStart="@drawable/ic_close"
                android:drawablePadding="16dp"
                android:padding="16dp"
                android:text="Đăng xuất"
                android:textColor="@color/color_error"
                android:textSize="16sp"
                android:textStyle="bold"
                android:gravity="center_vertical"
                android:layout_marginBottom="12dp"/>

        </LinearLayout>
    </ScrollView>

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"/>

</LinearLayout>